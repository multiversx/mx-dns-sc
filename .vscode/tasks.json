{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dns-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//dns/debug"
            },
            "type": "shell"
        },
        {
            "label": "user-mock-debug-build",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceFolder}//user-mock/debug"
            },
            "type": "shell"
        }
    ]
}